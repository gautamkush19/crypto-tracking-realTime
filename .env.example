NODE_ENV=development
PORT=10000

# Optional. CoinGecko's public demo endpoints can work without a key, but
# keeping the value server-side lets this app adopt a demo/pro key later
# without exposing it in browser bundles.
COINGECKO_DEMO_API_KEY=
COINGECKO_API_BASE=https://api.coingecko.com/api/v3
